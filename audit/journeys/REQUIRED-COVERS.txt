# Required journey coverage — critical features that must ALWAYS have an
# executable journey. One `covers:` tag per line. The gate (bin/journey-coverage.sh,
# local-CI stage 1.6) fails if any tag here is not claimed by a journey's
# `covers:` frontmatter, across Free (audit/journeys) and Pro (audit/pro/journeys).
#
# This is the coverage half of the regression defense: gates 1.4/1.5 stop bad code
# landing; this stops a critical feature shipping — or being silently dropped — with
# no end-to-end journey to catch a regression (the 2026-06-22 boost-drop class).
#
# Add a tag here when a feature becomes release-critical. Lines starting with # and
# blank lines are ignored.

boost-feed-promotion
dm-access-bug
battles
ai-feature-toggles
messaging-send-reliability
social-failure-paths
usage-ledger

# 2.0.0 member-expectation regressions (each mapped to a customer journey above).
protected-media-gate
album-privacy-pagination
album-owner-single-view
comment-post-id-zero
media-edit-persist
watermark-stamp-upload

# 2.1.0 safety. The gate that was supposed to stop a critical feature shipping
# uncovered had never been told that SAFETY is a critical feature — none of the
# tags above is about blocking, reporting, or moderation. Both 2.1.0 incidents
# went straight through it:
#
#   - reporting was dead on every install (403 everywhere, on Free and Pro),
#     while `wp mvs cert` passed 66/66;
#   - the block gate was applied to 3 of ~112 write endpoints, so a blocked
#     member could still favourite, share, battle, and story-view their blocker.
#
# These are cross-actor authorization bugs: correctness depends on a relationship
# between two identities. Nothing that dispatches one GET as one user can see
# them. The journeys behind these tags use two real identities and assert the 403.
block-enforcement
suspension-gate
report
reports-default-on
safety
