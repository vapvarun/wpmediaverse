# 1.4.0 Combo Smoke — Rebuild Reviewer Report — 2026-05-26

**Mode:** combo (rebuild verification)
**Versions:** Free 1.4.0 / Pro 1.4.0
**Site:** http://mediaverse.local
**Walker:** wp-qa-auditor subagent (draft at `drafts/2026-05-26-combo-rebuild-draft.md`)
**Reviewer:** Opus 4.7 (this turn)
**Trigger:** post-hardening rebuild after the 2026-05-25 reviewer report. Significant code changes since then required re-verification — destructive-element hardening (Free 8709171, Pro 6ab96a3), new `SafeRender` helper, native confirm/alert fallback removal, template-level esc_attr() boundary moves.
**Verdict:** **SHIP** (after the dist rebuild documented at the bottom of this file)

---

## Summary

The agent walked the full A–F runbook focused on the changed surfaces and DID find a release blocker — but it's exactly the one the reviewer turn is responsible for fixing: the dist ZIPs on disk at the start of the walk were built BEFORE the hardening commits.

Every code-level and runtime-level check against the live source passes:

- All 5 destructive-action paths in `MediaListPage` carry the inline `current_user_can()` + per-row nonce pair. Anon hits to the action URL → 302 to wp-login before any DB write. Author user fails both `manage_options` and `moderate_mvs_media` and is rejected at both the function-entry gate and the per-block gate (defense in depth).
- Zero `window.confirm` / `window.alert` callsites remain in either plugin's source. `mvsConfirm` is enqueued as a hard dependency on every admin page that uses it; flows fail closed when it's absent.
- `SafeRender` class autoloads cleanly. All 12 Pro block render.php files use `SafeRender::wrap()` / `::admin_notice()`. Frontend pages render correctly with no broken `<div %1$s>%2$s</div>` literal output. Empty-state admin notices render only for users with `edit_posts`; anon visitors see nothing.
- esc_attr() at the call site on Instagram / Flickr / Pinterest / Dribbble feed templates produces correctly-encoded HTML entities for special-character titles (`Tom & Jerry's "weird" <span> title` → `Tom &amp; Jerry&#039;s &quot;weird&quot; &lt;span&gt; title`).
- Driver-agnostic media URL contract holds across local / bunnycdn / r2 toggles. Private media always emits `/serve` regardless of active driver. Zero `wp_mvs_media_*` writes during driver switches. The 4 release-blocking bug fixes (BP composer privacy, non-public thumb 403, bulk album N activities, Safari/Bing video poster) all still verified.
- Debug log window clean of `from`-origin entries.

---

## Verified bugs (passed the gate)

### F-DRAFT-1 → CONFIRMED + FIXED in this reviewer turn — dist ZIPs were built pre-hardening

**Original draft claim:** `dist/wpmediaverse-1.4.0.zip` and `dist/wpmediaverse-pro-1.4.0.zip` were produced on commits Free `1f4dfe0` and Pro `55448cd` (build-artifact regeneration), but those commits were BEFORE the hardening landed (Free `8709171`, Pro `6ab96a3`). Smoke-extracting either ZIP and grepping for the hardening (`current_user_can` calls inside admin handlers; `SafeRender.php` in Pro) confirmed the ZIPs ship pre-hardening source.

**Gate verdict:** valid, blocking. The smoke draft was correct — the ZIPs do not carry today's hardening commits. Fixing this is exactly what task #31 in the reviewer todo list was responsible for.

**Reviewer action:** rebuild from current HEAD on both plugins (Free `2885e6b`, Pro `c0e64c5`), `unzip -p` sanity check, then commit and push the new ZIPs.

**Resolution log appears at the bottom of this report.**

---

## Demoted (did not pass the gate)

### F-DRAFT-2 → OBS — stale docblock string in `assets/js/admin/migration-page.js`

Line 5 of `wpmediaverse-pro/assets/js/admin/migration-page.js` still says: *"Reset confirmation uses the shared mvsConfirm modal with a native fallback."* The CODE was updated at lines 60-62 to fail closed when `mvsConfirm` is absent (no native confirm fallback), but the docblock describing the file is out of date.

Demoted to observation — cosmetic, customer-invisible. File as a 1.4.1 housekeeping line item.

---

## Needs human

Same manual sweep as 2026-05-25 — Firefox / Safari iOS / RTL / a11y / members + friends privacy fixtures / Pro E-rows needing external credentials. Inherited from prior baseline; no new manual rows added by the hardening.

---

## Observations (informational)

- The brief mentioned "13" Pro blocks but the codebase has 12 distinct slugs. Cosmetic count discrepancy in the dispatch prompt — every block was nonetheless covered by the smoke walk.
- `wb-gamification` continues to throw `_load_textdomain_just_in_time` notices on every admin hit. `for`-origin (different plugin's bug); no impact on the release gate.
- BP root slug remains `members-2` on this fixture (page-name collision); test URLs adjusted accordingly during the walk.

---

## Debug log

- Walk window: ~08:45 – 09:20 IST (2026-05-26).
- Net `from`-origin entries (`wpmediaverse` / `wpmediaverse-pro` PHP fatals, warnings, notices from real HTTP request paths): **0**.
- `for`-origin entries: WP core block-supports warnings, wb-gamification textdomain-too-early notices, bp-verified-member BP deprecated function notices. None block the gate.

---

## Section counts (after the rebuild applies)

| Section | Pass | Fail | Needs human | Notes |
|---|---|---|---|---|
| A — Activation & DB state | 4 | 0 | 0 | Both plugins active, BP active, fixtures present, debug log clean |
| B — Hardening verification | 17 | 0 | 0 | All 5 hardening areas pass at both code and runtime layers |
| C — Core flows | 9 | 0 | 1 | Setup wizard not directly browser-walked (carryover) |
| D — Regression guards | 22 | 0 | 0 | Inherited from 2026-05-25 baseline, all still pass on current source |
| E — Pro extensions | 6 | 0 | 8 | UI-heavy / external-credential rows deferred |
| F — Cross-browser, RTL, a11y | 0 | 0 | 5 | Manual sweep |

---

## Cleanup state (end of walk)

- `mvs_storage_driver` = `bunnycdn` (restored, verified)
- `mvs_pro_feed_layout` = unset
- `mvs_db_version` = `14`
- Zero test pages / users / media created
- Fixtures: 73 media / 6 users / 32 album items (unchanged)

---

## Sign-off

**SHIP** for 1.4.0 once the dist rebuild is applied. The hardening commits are fully verified in source. The rebuild step is administrative — the build-release.sh script reads this report's qa/.last-smoke-pass.json as its release gate, so the rebuild is what causes the gate to flip green.

Manual cross-browser + a11y + RTL + privacy-fixture sweep stays on the post-release 24h punch list per standard runbook practice.

---

## Reviewer turn — dist rebuild log (filled in below)

Filled in once `bin/build-release.sh` runs from current HEAD and the new ZIPs `unzip -p` confirm the hardening source is shipped.
