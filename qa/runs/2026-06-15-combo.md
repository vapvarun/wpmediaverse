# WPMediaVerse 1.7.0 COMBO Smoke — Final Report (Opus reviewer-gated)

**Date:** 2026-06-15
**Mode:** COMBO (Free + Pro both active)
**Site:** https://wb-media.local/ (connected Local site; URL derived from mysql_current_site, not hardcoded)
**Viewports:** 1280x800 Chromium (Playwright MCP). Mobile 390px + cross-browser deferred to manual (see Needs human).
**Test account:** admin / user ID 1 (`?autologin=1`)
**Free version:** 1.7.0  |  **Pro version:** 1.7.0  |  Lockstep MVS_PRO_MIN_FREE = 1.7.0
**Worker draft:** `qa/runs/drafts/2026-06-15-combo-draft.md` (Sonnet; not self-certified)

## Verdict: SHIP

Zero `from`-origin failures, zero `from`-origin debug.log entries, no PHP fatal. Green-pass JSON written to `qa/.last-smoke-pass.json`. The build-release gate (`bin/build-release.sh`) will accept it: `release_version` 1.7.0 matches both HEADs, `mode` combo, 0 from-origin failures.

Counts: 44 PASS (34 browser/DB-verified + 10 code-confirmed), 0 FAIL, 12 manual/HUMAN.

## Verified bugs (passed the gate)

None. No finding in the worker draft was a `from`-origin failure.

## Reviewer independent re-verification

Per the gate, Sonnet's PASS claims are drafts. Opus independently re-verified the highest-value, just-shipped items (not rubber-stamped):

- **Version equality at HEAD** — `MVS_VERSION` = 1.7.0 and `MVS_PRO_VERSION` = 1.7.0 (grep on both main files). Gate-required for the JSON.
- **Battle Win XP fix (all 5 edit sites intact):** `GamificationSettings` registers `mvs_pro_battle_win_xp` (default 100); `BattleService::create()` snapshots `xp_win` (line 105); `BattleService::xp_for_win()` resolver (line 679); `CompetePointsBridge` constructor takes `?BattleService $battles` + `case 'mvs_battle_win'` (lines 67/151); `Plugin.php:529` passes `$battle_service` to the bridge.
- **Streak daily_check fix:** keyset-paginated, `DAILY_BATCH_SIZE`=100 / `DAILY_MAX_PER_RUN`=2000, `daily_check( int $after_user_id = 0 )` with async continuation.
- **Static gates (both plugins):** WPCS exit 0, PHPStan OK, coding-rules pass, arch-checks pass, boot smoke OK.

## Demoted (did not pass as a bug)

- **B.gamification-admin (403 on `?page=mvs-gamification`)** -> Observation, by-design. There is no standalone gamification admin page; gamification settings live in Pro Settings > Competitions, and `mvs-challenges` / `mvs-tournaments` / `mvs-battles` are the real management pages. The worker tested a non-existent URL (and triaged its own 403 the same way). Not a missing-page bug.

## Needs human (manual_required)

- D.private-media-activity-row — fc31bf0 fix applies to NEW private uploads; seeded rows predate it. Code-confirmed + journey 16 is the regression test. Verify with one fresh private upload.
- D.streak-badge-aria — no seeded user has an active streak; badge renders only when streak > 0.
- E.group-dm — routes registered (WP-CLI confirmed); live authenticated POST not exercised (no DB writes this run).
- E.multi-collection-save — UNIQUE KEY confirmed in schema; live multi-save POST not exercised.
- C.anon.album-collection — album-to-lightbox chain needs manual verify.
- F.firefox / F.safari / F.rtl / F.a11y — Chromium-only session.
- G.390px-explore / lightbox / activity — mobile viewport visual checks not run.

## Observations (informational, not bugs)

- Worker ran at 1280x800; the runbook prefers 1440x900 + 390x844 for mobile rows. Mobile/cross-browser folded into manual_required (consistent with the prior 1.6.0 green pass, which also deferred F/mobile).
- 5 console errors across the session, all external/non-plugin: self-signed dev SSL on a REST poll, expired logout nonce from a prior Playwright session, two wrong test URLs, one auth-required GET. None `from`-origin.
- No `wp-content/debug.log` file exists.

## Release-gate handshake

`qa/.last-smoke-pass.json` (combo, 1.7.0, 0 from-origin failures) is the signed-off customer-flow certification. `bin/build-release.sh` reads it and will package; without it the build refuses.
