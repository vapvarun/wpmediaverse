{
    "name": "wbcomdesigns/wpmediaverse",
    "description": "A general-purpose WordPress media platform plugin.",
    "type": "wordpress-plugin",
    "license": "GPL-2.0-or-later",
    "require": {
        "php": ">=7.4"
    },
    "autoload": {
        "psr-4": {
            "WPMediaVerse\\": "includes/"
        }
    },
    "config": {
        "allow-plugins": {
            "dealerdirect/phpcodesniffer-composer-installer": true
        },
        "sort-packages": true
    },
    "require-dev": {
        "phpunit/phpunit": "^9.6",
        "yoast/phpunit-polyfills": "^2.0",
        "squizlabs/php_codesniffer": "^3.7",
        "wp-coding-standards/wpcs": "^3.0",
        "phpstan/phpstan": "^1.10",
        "szepeviktor/phpstan-wordpress": "^1.3",
        "php-stubs/wordpress-stubs": "^6.4"
    },
    "scripts": {
        "ci": "bash bin/local-ci.sh",
        "ci:quick": "bash bin/local-ci.sh --quick",
        "ci:no-journeys": "bash bin/local-ci.sh --no-journeys",
        "journeys": "bash bin/run-journeys.sh",
        "journeys:critical": "bash bin/run-journeys.sh --priority critical",
        "journeys:dry-run": "bash bin/run-journeys.sh --dry-run",
        "coding-rules": "bash bin/coding-rules-check.sh",
        "css-tokens": "bash bin/css-token-contract.sh",
        "template-styles": "bash bin/template-style-check.sh",
        "dup-gate": "bash bin/duplication-gate.sh",
        "dup-gate:bless": "bash bin/duplication-gate.sh --bless",
        "journey-coverage": "bash bin/journey-coverage.sh",
        "arch-checks": "bash -c 'if [ -x ../wpmediaverse-pro/bin/architecture-checks.sh ]; then bash ../wpmediaverse-pro/bin/architecture-checks.sh; else echo \"[arch-checks] Free/Pro contract script lives in the wpmediaverse-pro sibling; check out Pro alongside to run it.\"; fi'",
        "phpcs": "vendor/bin/phpcs --standard=phpcs.xml -p --warning-severity=0",
        "phpcs:fix": "vendor/bin/phpcbf --standard=phpcs.xml",
        "phpcs:full": "vendor/bin/phpcs --standard=phpcs.xml -p",
        "phpstan": "vendor/bin/phpstan analyze --no-progress --memory-limit=2G",
        "test:contract": "php -d opcache.enable_cli=0 vendor/bin/phpunit --filter SettingsContractTest",
        "test:unit": "php -d opcache.enable_cli=0 vendor/bin/phpunit",
        "install-hooks": "git config core.hooksPath bin/git-hooks && echo 'Pre-push hook installed (bypass with SKIP_LOCAL_CI=1)'"
    }
}
